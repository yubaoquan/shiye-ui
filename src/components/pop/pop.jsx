import React from 'react';
import PropTypes from 'prop-types';
import Base from '../base';

class Pop extends Base {
  render() {
    return <span className="shiye-pop">pop</span>
  }
}

export default Pop;
