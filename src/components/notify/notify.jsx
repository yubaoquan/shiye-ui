import React from 'react';
import Base from '../base';
import PropTypes from 'prop-types';

class Notify extends Base {
  render() {
    return (
      <div>building notify</div>
    );
  }
}

export default Notify;
